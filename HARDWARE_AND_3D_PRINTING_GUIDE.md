# DuduClock (ESP32-C3 + 2.4" ST7789) 硬件规格与 3D 打印外壳设计指南

本文档汇总了 DuduClock（嘟嘟天气时钟）当前已实机验证的硬件外设定义、物理布局以及 3D 打印外壳建模的关键尺寸与避坑要点。

---

## 🧭 一、 当前物理朝向与基准坐标系

当前固件已锁定为 **$320 \times 240$ 标准横屏模式**（ST7789 MADCTL = `0x68`，顺时针旋转 90°）：

```text
              +-----------------------------------------------+
              |  [0,0] TL                       TR [319,0]   |
              |                                               |
  [ Type-C ]  |               2.4" TFT LCD                    |
  (左侧 9点钟) |             (320 x 240 横屏)                 |
              |                                               |
              |  [0,239] BL                   BR [319,239]   |
              +-----------------------------------------------+
                             [ 桌面底座支撑 ]
```

* **Type-C 供电/数据接口**：位于设备**左侧（9点钟方向）**。
* **原点 `[0,0]`**：位于屏幕**左上角**。
* **X 轴向右递增**：`0 -> 319`。
* **Y 轴向下递增**：`0 -> 239`。

---

## 📐 二、 3D 打印外壳建模核心尺寸与公差

### 1. 屏幕视口（Bezel Window）
* **2.4 寸 ST7789 显示屏物理参数**：
  * **显示活动区（Active Display Area）**：宽约 `48.96 mm`，高约 `36.72 mm`
  * **屏幕玻璃总外轮廓（Glass Outline）**：宽约 `60.26 mm`，高约 `42.72 mm`，厚度约 `2.2 mm ~ 2.6 mm`
* **外壳前盖开窗建议**：
  * **视口开孔尺寸**：`49.5 mm × 37.2 mm`（四周单边预留 `0.25 mm ~ 0.3 mm` 溢出量，避免挡住边缘像素）。
  * **视口边缘设计**：推荐做 `45°` 内倒角（Chamfer）或 `1.0 mm` 圆角，提升视觉高级感并减少阴影。
  * **限位槽**：前盖内侧做 `60.8 mm × 43.2 mm × 2.8 mm` 沉台槽，放入屏幕后背部用泡棉或后盖筋位压紧，严禁螺丝直接压迫玻璃。

### 2. 接口与按键开孔
* **Type-C 接口开孔（左侧）**：
  * **开孔尺寸**：建议开 `10.5 mm × 6.5 mm` 椭圆/圆角矩形，外壳开孔壁厚建议削薄至 `1.2 mm` 或做漏斗形微沉台，保证各类较粗的快充线胶壳均可完全插到位。
* **板载按键（KEY / GPIO 8）**：
  * 板载按键在 PCB 上，如果要做外壳按键，可设计：
    1. **一体式悬臂按键**（Print-in-place flexure button）；
    2. 或顶盖开 `3.5 mm` 圆孔，放置独立注塑/打印按键帽。
* **温湿度传感器隔离舱（SDA=GPIO8, SCL=GPIO9）**：
  * ESP32-C3 芯片（80MHz~160MHz）和 LCD 背光持续工作会有局部发热（温升通常在 3~5℃）。
  * **隔离建议**：将 AHT20/SHT30 传感器板置于**外壳底部独立进风仓**，并在壳体下方与侧方开设百叶散热栅格，杜绝内部热量倒灌。

---

## 🖥️ 三、 桌面人体工学仰角与结构建议

```text
       \  <- 屏幕面板 (后仰 15° ~ 25°)
        \
         \
          \ 
           +------------+  <- 桌面水平线
             (仰角 65° ~ 75°)
```

1. **桌面最佳仰角**：外壳背部底座建议设计 **$65^\circ \sim 75^\circ$ 的仰角**（相对垂直面后倾 $15^\circ \sim 25^\circ$）。
   * 优势：坐在人体工学椅上平视时无需低头，同时能有效避开室内天花板日光灯的直射反光。
2. **后盖与固定方式**：
   * 推荐方案：前壳预埋 **4 个 M2 热熔铜螺母（Heat-set inserts，外径 3.2mm，长 3.0mm）**，后壳使用沉头 M2 螺丝锁紧。
   * 备选方案：上下/左右咬合的卡扣设计（Snap-fit，间隙留 `0.2mm`）。

---

## 🖨️ 四、 推荐切片与打印参数

| 参数项 | 推荐值 | 说明 |
| :--- | :--- | :--- |
| **打印材质** | **PETG** 或 **PLA+** | PETG 耐温性更好（适合热熔螺母），PLA+ 表面细节更佳 |
| **层高 (Layer Height)** | `0.16 mm` ~ `0.20 mm` | 首层 `0.20 mm`，后续 `0.16 mm` 保证弧面平滑 |
| **顶层熨烫 (Ironing)** | 开启正面熨烫 | 使正面边框表面如注塑般光滑平整 |
| **填充率 (Infill)** | `15% ~ 25%` | 配合 3 层以上外壁（Wall loops），刚性足够 |
| **支撑类型 (Support)** | 树状支撑（Tree Support） | 方便清理内部沉台与 Type-C 开孔处的悬空支撑 |

---

## 🔌 五、 硬件引脚速查表（供电路走线对照）

| 信号 | GPIO | 用途 | 状态 / 备注 |
| :--- | :--- | :--- | :--- |
| **SCLK** | `GPIO 2` | 屏幕 SPI 时钟 | **已验证（严禁改为 GPIO10）** |
| **MOSI** | `GPIO 3` | 屏幕 SPI 数据 | 已验证 |
| **CS**   | `GPIO 7` | 屏幕片选 | 已验证 |
| **DC**   | `GPIO 4` | 命令/数据选择 | 已验证 |
| **RST**  | `GPIO 5` | 屏幕硬件复位 | 已验证 |
| **BL**   | `GPIO 6` | 背光 NMOS 栅极 | 默认不操作即高亮；支持 5kHz PWM 调光 |
| **KEY**  | `GPIO 8` | 用户按键 | 低电平有效（内部上拉） |
| **SDA**  | `GPIO 8` | I2C 数据 | 温湿度传感器 |
| **SCL**  | `GPIO 9` | I2C 时钟 | 温湿度传感器 |
